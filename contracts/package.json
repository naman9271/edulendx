{
  "name": "edulendx-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for EduLendX platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:kwala": "hardhat run scripts/deploy.js --network kwala",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
